# Минимальный консервативный парный генератор силы без эквивариантности: решётка как репер в молекулярной динамике кристалла меди

> Рабочий черновик статьи, **draft v5** — структура под *Physical Review Materials* / *J. Chem. Phys.* / *Comput. Mater. Sci.* (классический формат Intro→Related→Methods→Results→Discussion→Conclusion). Собран из: введение v4, Методы (разделы 1–8, рис. 1), Results (рис. 2 сводная валидация + рис. 3 побочная проверка памяти, таблица 1), Related work, Abstract, Discussion/Limitations, Conclusion, Доступность кода и данных. Библиография [1]–[23].

## Аннотация

Мы предлагаем минимальный консервативный межатомный генератор силы для
молекулярной динамики кристалла: один разделяемый парный оператор строит скалярную
энергию, чётную по относительному смещению пары атомов, а силы получаются
автодифференцированием этого скаляра. Модель обучается **только по силам**, без
энергетических меток, и намеренно **отказывается от встроенной вращательной
эквивариантности**: при фиксированной ориентации кристалла решётка сама задаёт
репер, глобальные повороты не сэмплируются в NVT-динамике, поэтому эквивариантность
избыточна. Это даёт простой декартов вход (шесть каналов на пару), компактную
модель (< 0.5 млн параметров) и дешёвый шаг. Мы валидируем модель не по ошибке силы,
а по **коллективной динамике**: парный оператор, обученный на суперячейке 3×3×3
меди, переносится без дообучения на систему из 1000 атомов и воспроизводит
динамический структурный фактор S(q,ω), фононную дисперсию, радиальную функцию
распределения, распределение скоростей, скорость звука и теплоёмкость эталонной
траектории на основе метода погружённого атома (EAM); полная энергия остаётся стабильной на 100 пс. В качестве побочной проверки мы
спрашиваем, нужна ли модели ещё и память, и добавляем рекуррентное ядро с короткой
историей как контролируемую ось. Ответ — **отрицательный**: для марковского
кристалла история избыточна, и однокадровая безрекуррентная модель воспроизводит
динамику точнее и сохраняет энергию почти на порядок лучше, чем 3-кадровая
рекуррентная, при меньшем числе параметров. Это лишь подтверждает достаточность
простой базовой модели и согласуется с формализмом Мори–Цванцига, по которому
память проявляется только в немарковских режимах. Наконец, мы показываем, что то
же force-only построение восстанавливает и **механические** отклики: один
вириальный член в потере ($P=0$ в равновесном репере, без энергетических меток)
устраняет ложное давление ($-70\to-0.7$ ГПа) и делает решётку механически
устойчивой, а полный кубический тензор упругости, снятый из наклонов
акустических фононов прямо в рабочей 300 K-траектории, согласуется с эталоном в
пределах $\sim\!10\%$ ($B=124$ против $141$ ГПа) и правильно воспроизводит
нарушение соотношения Коши $C_{12}\neq C_{44}$.

## Введение

Машинно-обучаемые межатомные модели (MLIP) стали одним из основных способов
приблизить точность первопринципных расчётов к временам и размерам классической
молекулярной динамики. Определяющая черта этого класса — **локальность и
разделение весов**: потенциальная поверхность строится как сумма локальных
вкладов, зависящих от описания окружения атома, и один и тот же оператор
применяется к каждому атому. Именно это делает модель, обученную на нескольких
десятках атомов, применимой к системам произвольного размера. К этой линии
относятся high-dimensional neural network potentials Behler–Parrinello [1] и
Deep Potential Molecular Dynamics [2], а также графовые модели вроде SchNet [3].
Их общее физическое преимущество в том, что сила не предсказывается как
независимая векторная величина, а получается как **градиент скалярной функции**,
что накладывает сильное ограничение на поле сил и делает такие модели пригодными
для долгих MD-траекторий.

**Градиентная структура и обучение по силам.** То, что сила есть производная
скаляра, — не только удобная параметризация, но и самостоятельная линия методов.
В градиентно-доменном обучении (GDML [18]) поле сил моделируется напрямую как
градиент латентного энергетического ядра, а энергия восстанавливается
интегрированием; обучение идёт по силовым меткам. Систематический анализ
показывает, что производная (силовая) информация несёт больше сведений о
поверхности на единицу вычислений, чем энергетическая. В пределе, когда меток
энергии нет в принципе, скаляр становится чисто латентным: так устроено
согласование сил (force matching) в крупнозернистом моделировании, где сеть
параметризует потенциал средней силы и обучается только по силам (CGnet [19]).
Наша модель наследует именно эту постановку — консервативный скаляр,
существующий лишь через свой градиент, обучаемый без прямых меток энергии.

**Эквивариантность и её цена.** Современные E(3)-эквивариантные модели — NequIP
[4], Allegro [5], MACE [6] — и универсальные предобученные потенциалы (M3GNet
[7], CHGNet [8], MatterSim [9], Orb [10]) явно кодируют поворотные и
отражательные симметрии. Это резко повышает эффективность по данным, но требует
дорогой машинерии — сферических гармоник, тензорных произведений Клебша–Гордана,
высокого порядка взаимодействий на каждом ребре. Отказ от этой стоимости —
активное направление: усреднение по фреймам (frame averaging [20], FAENet [21]),
обучаемая канонизация [22] и, в самое последнее время, полностью
неэквивариантные потенциалы [23]. Все эти подходы, однако, **восстанавливают**
эквивариантность окольным путём — через репер, который на каждой конфигурации
вычисляется или обучается.

**Наша постановка: репер даёт сама решётка.** Мы рассматриваем динамику
конкретного кристалла при фиксированной ориентации. В таком состоянии
вращательная симметрия спонтанно нарушена: решётка сама задаёт физический
репер, а глобальные повороты в NVT-динамике около равновесия не сэмплируются.
Поэтому эквивариантность здесь не приближается, а становится **избыточной** —
индуктивное смещение под неизвестную ориентацию не нужно, когда ориентация
задана системой. Это позволяет оператору действовать напрямую над блоком поля
смещений $\mathbf{u}_i$ в решёточном репере с простым декартовым входом: признаки
пары — это $[\,\mathbf{R}^{\text{ref}}_{ij}/a_0,\ (\mathbf{u}_j-\mathbf{u}_i)/a_0\,]$,
шесть каналов, без угловых базисов и сферических гармоник. Единственная явно
вшитая симметрия — чётность пары $U(\mathbf{x})=\tfrac12(f(\mathbf{x})+f(-\mathbf{x}))$.
Полная модель — один разделяемый парный оператор объёмом менее $0{,}5$ млн
параметров, что на один–два порядка компактнее типичных эквивариантных MLIP и
даёт заметно более дешёвый шаг МД (плотные умножения матриц $\le 256\times256$,
без разреженных тензорных ядер).

**Что мы строим.** Модель не предсказывает силу напрямую: она возвращает чётный
скалярный парный генератор $U_{ij}$, а сила получается автоматическим
дифференцированием этого скаляра по текущим относительным смещениям и
распределяется по парам так, что суммарная сила равна нулю по построению.
Обучение идёт **только по силам/ускорениям**, без меток энергии. Мы намеренно
сводим архитектуру к минимуму — парный, неэквивариантный, консервативный
генератор в решёточном репере, — чтобы проверить, насколько далеко такая простая
конструкция способна воспроизвести **коллективную динамику** кристалла. Поэтому
и качество оценивается не по одношаговой ошибке сил, а по динамическим
наблюдаемым на длинных прогонах (rollout): в первую очередь по динамическому
структурному фактору $S(q,\omega)$, а также по температурной стабильности,
распределению скоростей и поведению полной энергии $E_{\text{tot}}$. Мы берём
медь — простой ГЦК-эталон с хорошо известным фононным спектром, дающим
независимую сверку.

**Память как побочная ось.** Базовая модель работает с одним кадром — силу
определяет мгновенная конфигурация относительных смещений. Отдельно мы задаёмся
вопросом, изменит ли что-нибудь добавление короткой **истории**: вместо одного
кадра подать в оператор несколько последовательных через рекуррентное ядро. Эта
ветка мотивирована линией memory-in-MD — рекуррентными моделями траекторий [11]
и, более фундаментально, формализмом обобщённого уравнения Ланжевена и
Мори–Цванцига, где исключённые степени свободы порождают ядро памяти [12]. Для
марковского эталона (EAM — консервативный потенциал, сила полностью определена
мгновенной конфигурацией) такая память, вообще говоря, не обязана помогать, и мы
рассматриваем длину истории как **контролируемую переменную**: сопоставляя
рекуррентное ядро с базовым безрекуррентным при том же окне, мы проверяем, нужна
ли история вообще. Забегая вперёд, она оказывается избыточной — что лишь
подтверждает достаточность простой базовой модели; память ожидаемо стала бы
существенной в диссипативных и коарс-грейн-режимах в духе Мори–Цванцига, вне
рассматриваемого здесь идеального кристалла.

**Вклад работы.**
1. **Минимальный консервативный парный генератор**, обучаемый только по силам:
   чётный скаляр $U_{ij}$ существует лишь через свой градиент, метки энергии не
   используются.
2. **Сознательный отказ от эквивариантности** за счёт репера, заданного решёткой:
   простой декартов вход, компактная модель ($<0{,}5$ млн параметров) и дешёвый
   шаг — в отличие от подходов, восстанавливающих эквивариантность через
   вычисляемый или обучаемый репер [20–23].
3. **Единица разложения — трансляционная ячейка, а не атом**: в отличие от
   стандартного атомоцентрированного разложения $E=\sum_i E_i$, скользящее окно
   $3\times3\times3$ предсказывает силы сразу для всей центральной ячейки и
   движется по трансляциям решётки; это позволяет переносить обученный на
   $3\times3\times3$ оператор на решётку $10\times5\times5$ ($1000$ атомов) без
   переобучения и естественно опирается на решётку как репер.
4. **Валидация по коллективной динамике**: модель оценивается по $S(q,\omega)$ и
   термодинамически мотивированным наблюдаемым на длинных траекториях, а не по
   локальной ошибке сил.
5. **Память как контролируемый результат**: длина истории исследуется как
   отдельная ось, что позволяет разграничить марковский режим кристалла и
   режимы, где память ожидаемо необходима (GLE / Мори–Цванциг).
6. **Механический якорь: от сил к упругому тензору**: один вириальный член в
   потере ($P=0$ в равновесном репере, без энергетических меток) устраняет
   ложное давление и делает решётку устойчивой, а полный кубический тензор
   упругости, снятый из наклонов акустических фононов рабочей траектории,
   согласуется с эталоном в пределах $\sim\!10\%$ и правильно воспроизводит
   нарушение соотношения Коши $C_{12}\neq C_{44}$.

---

## Связанные работы и позиционирование

Наш подход стоит на пересечении трёх линий исследований; ниже мы явно
разграничиваем, что заимствуем, а что делаем иначе.

### Градиентно-доменное обучение и обучение только по силам

Идея параметризовать не силы напрямую, а скалярный потенциал и брать силы как его
градиент восходит к градиентно-доменному машинному обучению (GDML [18] и его
симметризованная версия sGDML), где предсказываются именно силы как градиенты
энергии, что гарантирует консервативность и бездивергентность силового поля.
Обучение по силам без энергетических меток используется и в грубозернистом
моделировании: CGnet [19] строит потенциал средней силы, обучаясь на силах, а
работы по многочастичному грубому зернению (MS-CG) формализуют извлечение
консервативных сил из проекции. NewtonNet и близкие архитектуры также используют
силы как первичный обучающий сигнал.

От этой линии мы берём базовый принцип — **энергия существует только как скаляр,
из которого силы получаются автодифференцированием, а обучение идёт только по
силам** — но переносим его с молекул и грубозернистых систем на **кристаллическую
решётку**, где скаляр является парным и чётным по смещению, а суммарная сила в
паре зануляется по построению.

### Эквивариантность и её альтернативы

Доминирующая линия межатомных потенциалов последних лет — **эквивариантные**
модели: SchNet, NequIP [4], Allegro [5], MACE [6], а также универсальные
потенциалы M3GNet [7], CHGNet [8], MatterSim [9], Orb [10]. Они встраивают
вращательную симметрию SO(3) через сферические гармоники и тензорные произведения,
что даёт высокую точность и переносимость, но ценой существенной вычислительной
стоимости (тензорные произведения, миллионы параметров).

Параллельно развивается направление, ставящее под вопрос необходимость *встроенной*
эквивариантности: усреднение по реперам (frame averaging [20]), FAENet [21] и
обучаемая канонизация [22] достигают инвариантности/эквивариантности *приближённо*
и апостериорно, без тензорной алгебры; недавняя работа [23] показывает, что
конкурентные межатомные потенциалы можно обучать вовсе без явной эквивариантности.

Наш выбор ближе к последней линии, но отличается принципиально: мы **не приближаем**
эквивариантность и не усредняем по реперам. В задаче с фиксированной ориентацией
кристалла вращательная симметрия спонтанно нарушена — **решётка сама задаёт репер**,
а глобальные повороты не сэмплируются в NVT-динамике при фиксированной ячейке.
Поэтому эквивариантность не нужна: вход остаётся простым декартовым (6 каналов на
пару — опорный вектор решётки и относительное смещение), модель компактна (< 0.5 M
параметров) и дёшева по шагу, без сферических гармоник и коэффициентов
Клебша–Гордана.

### Память и немарковость в молекулярной динамике

Рекуррентные и историю-зависимые модели применялись в МД для захвата
немарковских эффектов: LSTM-модели динамики [11] и подходы на основе обобщённого
уравнения Ланжевена (GLE) с обучаемой памятью [12], опирающиеся на формализм
Мори–Цванцига, где интеграл памяти возникает при проекции на редуцированные
координаты. В этих постановках память **физически необходима**: она кодирует
эффект исключённых степеней свободы.

Наш вклад по этой оси — **контролируемый отрицательный результат**. Мы вводим
память как явную архитектурную ось (рекуррентное временное ядро с историей из
нескольких кадров) и абляцией показываем, что для бездиссипативной динамики
идеального марковского кристалла память избыточна и даже вредна. Это не
противоречит [11,12], а очерчивает границу: память полезна там, где динамика
редуцированных координат немарковская, и не даёт выигрыша там, где мгновенное
локальное окружение полностью определяет силу.

## Методы

Раздел описывает модель так, как она реализована в коде ветки `Paper`
(класс `CrystalPairEnergyRNNNet` в `base_classes/edge_rnn_predictor.py`),
и воспроизводит те значения гиперпараметров, при которых была получена
эталонная модель статьи.

![Рис. 1. Архитектура модели: локальная геометрия пары в блоке $3\times3\times3$ (один кадр в базовой модели; короткая история — в рекуррентном расширении) кодируется разделяемым оператором в чётную скалярную парную энергию $U_{ij}$, градиент которой по относительным смещениям даёт консервативную парную силу; суммирование по 18 соседям и парный разброс обеспечивают строго нулевую суммарную силу. На схеме показан покадровый двунаправленный рекуррентный вариант временной оси.]({{artifact:art_ac3253cf-6eef-4284-85a3-2b2b91fe4779}})

### 1. Постановка задачи и представление системы

Рассматривается кристалл меди с ГЦК-решёткой (4 атома на условную
ячейку) в периодических граничных условиях. Динамика задаётся полем
смещений атомов от их равновесных узлов $\mathbf{u}(t)$; референсные
траектории получены классической молекулярной динамикой (см. п. 6).

Модель не работает со всем кристаллом сразу. Для каждого центрального
атома формируется локальное окружение фиксированной геометрии — «стенсил»
из двух координационных сфер ГЦК-решётки: 12 ближайших соседей и 6
соседей второй сферы, всего **18 соседей** на атом. Стенсил строится один
раз по референсным (равновесным) положениям с учётом минимального образа
и радиуса отсечки $r_c = 1.05\,a_0$, где $a_0$ — параметр решётки
(в коде — `build_edge_stencil`, `neighbor_shells=2`, `cutoff_scale=1.05`).
Список соседей и опорные (равновесные) векторы связей $\mathbf{R}^{\text{ref}}_{ij}$
после этого фиксированы.

Входом модели служит короткая **история** из трёх последовательных
кадров смещений (`sequence_length = 3`), заданная на блоках $3\times3\times3$
условных ячеек вокруг каждого центра. Для каждой ориентированной пары
«центр–сосед» $(i,j)$ на каждом кадре строится вектор признаков. В
основном (используемом в статье) режиме `ref-plus-delta` он состоит из
**шести каналов**:

$$
\mathbf{x}_{ij}(t) = \Big[\; \mathbf{R}^{\text{ref}}_{ij}/a_0,\quad
\big(\mathbf{u}_j(t)-\mathbf{u}_i(t)\big)/a_0 \;\Big],
$$

то есть первые три канала — постоянный опорный вектор связи (в единицах
$a_0$), а последние три — текущее относительное смещение соседа
относительно центра, тоже в единицах $a_0$
(`edge_sequence_features_from_patches`,
`_feature_channels_for_temporal_input_mode = 6`). Такое разделение на
«опорную» и «динамическую» части существенно: силы получаются
дифференцированием только по динамическим каналам (п. 4), тогда как
опорный вектор задаёт геометрический контекст пары.

Все длины нормированы на $a_0$; шкала масштабируется обратно при переходе
к физическим единицам.

#### Скользящий блок $3\times3\times3$

Базовая вычислительная единица модели — блок из $3\times3\times3$ условных
ячеек. Силовое поле предсказывается **по координатам всех атомов блока,
но только для его центральной области** (центральная ячейка $(1,1,1)$):
атомы центральной ячейки видят полное окружение из двух координационных
сфер, целиком лежащее внутри блока, поэтому именно для них ускорение
вычисляется корректно. Внешние ячейки блока играют роль граничного
контекста и сами по себе не являются мишенью.

Это разделяет **геометрию обучения и геометрию инференса**. Обучается
модель на блоках, вырезанных из небольшого кристалла $3\times3\times3$
(с периодическими границами): каждый обучающий пример — блок с известным
ускорением центральных атомов. При инференсе тот же самый разделяемый
оператор применяется к кристаллу **произвольного размера**: окно
$3\times3\times3$ последовательно центрируется на каждой ячейке большого
кристалла (с учётом периодичности), и предсказанные для центральной
области вклады собираются в полное силовое поле всех атомов
(`predict_full_accelerations`, `_full_pair_scatter_indices`). Поскольку
локальный оператор один и тот же независимо от положения окна, обученная
на $3\times3\times3$ модель без переобучения переносится на большие
кристаллы — в нашем случае на решётку $10\times5\times5$ ячеек
($1000$ атомов, обозначение `1055`).

**Единица разложения — ячейка, а не атом.** Стоит подчеркнуть, в чём это
отличается от стандартной атомоцентрированной парадигмы. Со времён
Behler–Parrinello [1] локальность в МОП реализуется через разложение полной
энергии по *атомам*, $E=\sum_i E_i(\{\text{окружение атома }i\})$, и инференс
на большом кристалле есть суммирование одноатомных вкладов, каждый в своей
сфере обрезания; на этом принципе построены и потенциалы Deep Potential [2],
и графовые/эквивариантные модели [3–6]. Сам принцип near-sightedness мы
наследуем без изменений. Меняется **гранулярность**: единицей предсказания у
нас служит не атом, а **трансляционная ячейка** — скользящее окно выдаёт силы
сразу для всех атомов центральной ячейки $(1,1,1)$, а само окно движется по
*трансляциям решётки*, ячейка за ячейкой, а не по атомам в произвольно
ориентированных окружениях. Такой выбор органичен именно в кристалле и прямо
связан с главным тезисом работы (следующий раздел): фиксированная решётка сама
задаёт репер, в котором ячейка окна имеет однозначную ориентацию, поэтому
плитка «ячейка = единица» корректно определена без какой-либо эквивариантности.
В неупорядоченной среде (жидкость, аморф) такая трансляционная плитка смысла бы
не имела, и разложение по атомам было бы единственным вариантом; в кристалле же
ячейка — естественная единица генерации сил.
Отдельные ингредиенты этого выбора имеют прецеденты: перенос модели, обученной
на малой суперячейке, на бо́льшую систему по одним лишь силам показан для
графовых силовых полей с прямым (безэнергетическим) предсказанием силы [24], а
разложение вклада в энергию до уровня *пар* $E_{ij}$ (вложенное, впрочем, в
поатомную сумму $E=\sum_i E_i$) используется в Allegro [5]. Новым здесь мы
считаем не сами эти приёмы, а их сборку вокруг **ячейки как единицы генерации
сил** в фиксированном решёточном репере: единица предсказания — не атом
(как в [1–6]) и не огрублённая группа-бусина с потерей разрешения
(как в грубозернистых МОП [19]), а трансляционная ячейка при полном атомистическом
разрешении. Такой комбинации, насколько нам известно, для МД кристалла ранее
не применяли.

### 2. Кодировщик признаков пары (базовый и рекуррентное расширение)

**Базовая модель безрекуррентна:** признаки одного кадра пары проходят
через разделяемый (общий для всех пар и всех атомов) полносвязный
кодировщик (`temporal_architecture = "mlp"`), возвращающий вектор
признаков. Это рабочая конфигурация статьи.

Отдельно мы реализуем **рекуррентное расширение по временной оси** —
чтобы проверить побочный вопрос о роли памяти (§5, абляция). В нём тот же
разделяемый блок отображает не один кадр, а короткую **историю** пары в
один вектор признаков; используется **покадровый** вариант
(`temporal_architecture = "frame-layered"`, класс
`_FrameLayerTemporalEncoder`).

В отличие от стандартного `nn.RNN(num_layers=N)`, где каждый слой
применяется ко всем временным шагам, здесь **на каждый кадр истории
приходится своя рекуррентная ячейка**:

$$
h_i = \text{cell}_i(x_i, h_{i-1}), \qquad i = 1,\dots,L,
$$

поэтому число «слоёв» $L$ жёстко равно длине истории
($L = \text{`rnn_layers`} = \text{`sequence_length`} = 3$, отсюда метка
`rl3`). Ядро **двунаправленное** (`bidirectional=True`): симметричная
обратная ветвь проходит кадры от последнего к первому своим набором
ячеек. Тип ячейки — ванильная Элмановская `RNNCell` (в коде также
доступны `GRUCell`/`LSTMCell`); скрытая размерность $h = 128$
(метка `h128`).

Считывание — режим `final-hidden`: конкатенация финальных скрытых
состояний прямой и обратной ветвей, что даёт вектор ширины
$2h = 256$.

### 3. Скалярная парная энергия с чётной симметрией

Вектор считывания пропускается через небольшую голову
(`Linear(256,256) → ELU → Linear(256,1)`), возвращающую **скаляр** —
парную энергию $U_{ij}$ (класс `_EvenPairEnergyRNN`).

Чтобы энергия была инвариантна к перестановке порядка атомов в паре
(развороту ориентированной связи), выход явно **симметризуется по
чётности**:

$$
U(\mathbf{x}) = \tfrac{1}{2}\big(f(\mathbf{x}) + f(-\mathbf{x})\big),
$$

где $f$ — «сырой» выход сети. Таким образом $U(\mathbf{x}) = U(-\mathbf{x})$
по построению. Скаляр домножается на обучаемо согласованный масштаб
`energy_output_scale`, приводящий его к шкале ускорений
(строится из статистики целевых ускорений, п. 5).

### 4. Консервативные силы через градиент скаляра

Ключевой элемент модели — силы (ускорения) получаются **не отдельной
головой, а дифференцированием скаляра**. Для пары с текущим динамическим
вектором $\mathbf{x}$ вклад в ускорение центрального атома есть градиент
суммарной парной энергии по динамическим каналам последнего кадра
истории:

$$
\mathbf{a}_{ij} = \frac{1}{a_0}\,
\frac{\partial\, U_{ij}}{\partial \big[(\mathbf{u}_j-\mathbf{u}_i)/a_0\big]}
\Big|_{\text{последний кадр}},
$$

а ускорение центрального атома — сумма по всем 18 соседям:
$\mathbf{a}_i = \sum_j \mathbf{a}_{ij}$
(`_pair_contributions_from_features`, `_center_acceleration_from_features`;
градиент берётся `torch.autograd.grad` по срезу `[..., -1, 3:6]`, деление
на $a_0$ — это цепное правило из-за нормировки признаков). Поскольку сила
есть градиент скалярного потенциала при фиксированном контексте истории,
локальное силовое поле **консервативно по построению** для данной
истории.

Во время обучения градиент вычисляется со вторым порядком
(`create_graph=True`), так что параметры сети остаются связаны с функцией
потерь через градиент энергии (сила обучается как производная скаляра,
а не как самостоятельный выход).

При инференсе на полном кристалле используется **парный разброс**
(`pair_scatter_inference=True`): каждая уникальная периодическая пара
вычисляется один раз, и её вклад добавляется как $+\mathbf{f}$ центру и
$-\mathbf{f}$ соседу (`_scatter_pair_contributions`, `np.add.at`). Это
даёт **строго нулевую суммарную силу на каждом шаге** — сохранение
импульса заложено в архитектуру, а не достигается пост-обработкой.

### 5. Обучение

**Целевая величина.** Модель обучается **только по силам** — меток
энергии нет. Из референсной МД берутся силы $\mathbf{f}$ (компоненты
`fx fy fz` из дампа LAMMPS), переводятся в дискретные ускорения
($\mathbf{a} = \mathbf{f}/m$, `forces_to_discrete_accelerations`), и
целью служит ускорение центрального атома блока
(`target_mode = "force"`, `_target_force_acceleration`).

**Нормировка.** Целевые ускорения нормируются глобально — одной парой
(среднее, СКО) по всему набору (`acceleration_normalization = "global"`).

**Функция потерь и оптимизатор.** Основная потеря — среднеквадратичная
ошибка (`nn.MSELoss`) между предсказанным (нормированным) и целевым
ускорением. В коде предусмотрен ряд вспомогательных потерь (моменты
смещений, спектральные штрафы, curl-штраф и т. п.), но в эталонной
конфигурации их веса равны нулю. Оптимизатор — Adam,
скорость обучения $10^{-3}$, размер батча 64, 50 эпох, перемешивание
(`DataLoader(shuffle=True)`).

**Отбор моделей.** Обучается пакет из нескольких кандидатов с разными
инициализациями; каждый после обучения прогоняется в режиме
авторегрессионного развёртывания (rollout) на малом кристалле, и
качество оценивается метрикой **`mean_norm`** — усреднённым по нескольким
прогонам $L_2$-расстоянием между спектрами $S(q,\omega)$ (спектр
мощности продольного тока) предсказанной и референсной траекторий; меньше
— лучше (`evaluate_model`, `get_sqw_default`, $\text{DT}=0.02$,
$\text{STEP}=10$). Эталонная модель статьи имеет
$\texttt{mean\_norm} = 0.6129$, что заметно лучше лучшей модели
предыдущего (плоского локального RNN) семейства
($\texttt{mean\_norm} \approx 2.69$).

Полное имя эталонной модели (обучена 2026-06-22) кодирует конфигурацию:
`h128`, `rl3`, `readoutfinalhidden`, `temporalframelayered`,
`inputrefplusdelta`, `bidir`, `shells2`, `n18`, `targetforce`,
`accnormglobal`.

### 5a. Механический якорь: вириальный член в потере

Базовая модель (п. 1–5) обучена и валидирована на воспроизведение
динамики при тепловых амплитудах. Чтобы проверить, восстанавливает ли та
же force-only конструкция *механические* отклики (равновесное давление,
упругий тензор), мы дообучаем ту же заголовочную архитектуру
(вход `ref-plus-delta`, п. 1) с одним добавленным членом потери —
вириальным якорем. Он не меняет ни вход, ни представление энергии, а лишь
фиксирует свободную аддитивную константу; влияние на динамику мало
(корреляция $S(q,\omega)$ падает лишь с $0.938$ до $0.930$).

**Якорь давления.** Force-only обучение фиксирует силы, но оставляет
свободной аддитивную константу энергии и, как следствие, абсолютное
давление в равновесном репере: без дополнительного условия обученная
модель даёт большое ложное давление в узлах решётки (до
$\approx -70$ ГПа). Мы добавляем к потере один физически прозрачный член,
занулящий вириальное давление в равновесной конфигурации:
$$
\mathcal{L}_{\text{anchor}}
= w \left[\frac{1}{3V}\,\mathrm{tr}\,\mathbf{W}\right]^2_{\mathbf{u}=0},
\qquad
\mathbf{W}=\tfrac{1}{2}\sum_{ij}\mathbf{R}_{ij}\otimes\mathbf{f}_{ij},
$$
где $\mathbf{W}$ — тот же вириал, что читается из калиброванного автограда
сил (согласованность проверена численно, отношение loss-пути к
инференс-пути $=1.00004$), целевое давление равно нулю, а вес
$w=10^4$. Член вычисляется на равновесной решётке ($\mathbf{u}=0$) и не
требует энергетических меток — это по-прежнему обучение только по силам
плюс *одно* условие равновесия. Якорь снимает ложное давление
($-70\to-0.7$ ГПа) и делает решётку механически устойчивым минимумом по
объёму, после чего упругий тензор извлекается из акустических фононов
рабочей траектории (Результаты).

### 6. Данные

Референсная динамика — классическая молекулярная динамика меди с
потенциалом погружённого атома (EAM [16], `Cu_u3.eam`) на ГЦК-решётке; шаг
записи траектории $0.002$ пс (`prepare_crystal_data.py`,
`--dt-ps 0.002`). Обучающий набор `data333_force.npz` готовится из дампа
LAMMPS для суперячейки $3\times3\times3$ (`--crystal-shape 3 3 3`,
`--train-supercell-shape 3 3 3`, `--unit-cell-atoms 4`,
`--sequence-length 3`) с обязательным включением сил
(`--include-forces`, `--require-forces`). Обучающими примерами служат
блоки смещений $3\times3\times3$ (со скользящим окном, см. п. 1),
выбираемые вдоль траектории; мишень — ускорения центральной области
блока. Обучающий набор эталонной модели — $30\,000$ последовательных
троек кадров (метка `d30k` в имени модели): каждая тройка задаёт
историю длины 3 и соответствующее целевое ускорение.

### 7. Инференс: молекулярная динамика с обученной моделью

Обученная модель оборачивается в ASE-калькулятор [13]
(`ase_copper_calculator.py`): он поддерживает скользящую историю
смещений, на каждом шаге предсказывает ускорения (и, для энергетической
модели, скалярную парную энергию), переводя их в силы в эВ/Å через массу
меди ($m = 63.546$ а.е.м.). Прогон ведётся в ансамбле NVT с термостатом
Бусси [14] (`ase.md.bussi.Bussi`) при $T = 300$ К и времени связи $200$ фс
(метка `bussi200`).

Диагностика энергии в статье строится на **полной энергии**
$E_{\text{tot}} = E_{\text{pot}}^{\text{model}} + E_{\text{kin}}$, где
$E_{\text{pot}}^{\text{model}}$ — сумма обученных скалярных парных
энергий $U_{ij}$, переведённая в эВ. Следует помнить, что этот скаляр —
обученная потенциало-подобная величина, а не истинная потенциальная
энергия EAM: он обучается лишь косвенно, через своё производное (силу).

Главный длинный прогон выполнен на увеличенном кристалле
$10\times5\times5$ ячеек (в пайплайне обозначен `1055`, `data1055.npz`) —
это $1000$ атомов (4 атома на ячейку), то есть модель, обученная на
блоках из кристалла $3\times3\times3$, применялась скользящим окном к
решётке, много большей обучающей. Прогон длился $500\,000$ шагов;
корреляция $S(q,\omega)$ с референсом составила $\approx 0.8207$.

### 8. Инструменты и вычислительная среда

Модель и обучение реализованы на **PyTorch** [17] (рекуррентные ячейки
`nn.RNNCell`/`GRUCell`/`LSTMCell`, автодифференцирование для консервативных
сил), с использованием **NumPy** для сборки стенсила и парного разброса и
**tqdm** для контроля обучения. Референсная динамика и валидация —
**ASE** (интегратор и термостат Бусси) и **LAMMPS** [15] с EAM-потенциалом
меди. Обучение и длинные прогоны выполнялись на кластере под управлением
**SLURM** (раздел `gold-batch`), в conda-окружении `torch`.

## Результаты

Обученный парный оператор применяется как ASE-калькулятор в NVT-динамике
(термостат Bussi, τ = 200 фс, T = 300 K) на суперячейке Cu FCC 10×5×5 = 1000
атомов в течение 100 пс (50 000 шагов, dt = 2 фс). Оператор обучен на суперячейке
3×3×3 и переносится на систему в ~37 раз большего размера без дообучения. Все
диагностики сравниваются с эталонной траекторией той же системы под потенциалом
EAM (`Cu_u3.eam`).

![Рис. 2. Валидация коллективной динамики однокадровой модели против EAM-эталона (Cu FCC, 1000 атомов, 100 пс): (a) пиковая фононная дисперсия $\omega(q)$ из $S(q,\omega)$; (b) радиальная функция распределения $g(r)$; (c) дрейф полной энергии для однокадровой и 3-кадровой моделей; (d) распределение модуля скорости.]({{artifact:art_8f0a63ae-8d24-4a7f-bf9c-75d80175dea5}})

### Коллективная динамика и равновесные наблюдаемые

Ключевая проверка — **динамический структурный фактор** S(q,ω), чувствительный
к фононной дисперсии, то есть к согласованности сил вдоль коллективных мод, а не
к поточечной ошибке силы. Пиковая дисперсия ω(q) продольной ветви, восстановленная
из S(q,ω), совпадает с эталоном во всём доступном диапазоне q (рис. 2a);
интегральная корреляция карты S(q,ω) с эталоном составляет **0.94** для
однокадровой модели.

Равновесные и структурные наблюдаемые воспроизводятся с высокой точностью и не
различают модели:

- **Радиальная функция распределения** g(r): корреляция с эталоном > 0.99999,
  положения и высоты всех координационных пиков совпадают (рис. 2b).
- **Распределение скоростей** максвелловское, средний модуль ⟨|v|⟩ = 3.165 против
  эталонных 3.171, покомпонентная дисперсия совпадает до третьего знака (рис. 2d).
- **Термостатическая температура** удерживается на 300.6 K при цели 300 K.
- **Скорость звука**, извлечённая из наклона ω(q) при q → 0, даёт отношение к
  эталону 0.886.
- **Теплоёмкость** c_v ≈ 3.4 kB/атом (детрендированная оценка по флуктуациям
  полной энергии) согласуется с классическим пределом Дюлонга–Пти 3.0 kB/атом.

**Сохранение энергии.** Хотя модель обучена только по силам и энергия существует
лишь как скалярный потенциал, из которого силы берутся градиентом, полная энергия
в NVT-прогоне остаётся стабильной: дрейф полной энергии составляет 0.0011 эВ/пс
(|ΔE_tot| = 0.67 эВ за 100 пс на 1000 атомов, рис. 2c). Консервативная
конструкция силы — градиент чётного скаляра с занулением суммы сил в паре —
обеспечивает отсутствие систематической накачки энергии на длинных временах.

### Побочная проверка: нужна ли память

Установив, что базовая однокадровая модель воспроизводит динамику, мы отдельно
проверяем побочный вопрос — изменит ли что-нибудь добавление истории. Мы
сравниваем при идентичном протоколе две модели: **3-кадровую рекуррентную** (RNN, рекуррентная нейросеть; двунаправленное
ядро на ванильной Элмановской `RNNCell`, вход `ref-plus-delta`, история из 3 кадров) и **однокадровую
безрекуррентную** (MLP, полносвязная сеть; `temporalmlp`, один кадр, без истории и без рекуррентности).
Обе имеют одинаковый стенсил n18, скрытую размерность h = 128 и обучены только
по силам на одних и тех же данных.

Результат недвусмыслен и суммирован на рис. 3 и в таблице 1. **Однокадровая
модель превосходит рекуррентную сразу по двум главным осям:**

![Рис. 3. Абляция памяти: однокадровая безрекуррентная модель против 3-кадровой рекуррентной при идентичном протоколе. Слева — карта $S(q,\omega)$; в центре — модуль дрейфа полной энергии $|\Delta E_\mathrm{tot}|$; справа — метрика отбора mean_norm на коротком rollout.]({{artifact:art_b0bbfae8-291a-42c5-8e2a-59b4d5bf428c}})

| Диагностика | MLP (1 кадр) | RNN (3 кадра) |
|-------------|-------------:|--------------:|
| S(q,ω) корреляция | **0.9376** | 0.8261 |
| дрейф E_tot, эВ/пс | **0.00105** | 0.00746 |
| \|ΔE_tot\| за 100 пс, эВ | **0.671** | 1.278 |
| ⟨\|v\|⟩ (эталон 3.171) | 3.165 | 3.174 |
| RDF корреляция | 0.999996 | 0.999992 |
| скорость звука, отношение | 0.886 | 0.890 |

*Таблица 1. Диагностики однокадровой и 3-кадровой моделей при идентичном
протоколе (Cu FCC, 1000 атомов, 100 пс, Bussi τ = 200 фс). Полужирным —
существенно лучшее значение; для равновесных наблюдаемых (нижние строки) модели
неразличимы.*

Однокадровая модель воспроизводит коллективную динамику точнее (S(q,ω) 0.94
против 0.83) и сохраняет энергию почти на порядок лучше (дрейф в ~7 раз меньше),
при этом равновесные наблюдаемые — распределение скоростей, RDF, скорость звука —
у обеих моделей совпадают с эталоном и между собой. Важно, что **однокадровый
энкодер меньше по числу параметров** (один Linear→ELU против рекуррентной ветви,
разворачиваемой на 3 кадра в двух направлениях): выигрывает *меньшая* модель, что
усиливает вывод.

**Честная оговорка о метрике отбора.** Единственная величина, по которой
рекуррентная модель оказывается лучше, — это `mean_norm` (L2-расстояние S(q,ω) на
коротком rollout), 0.613 против 0.653, — ровно та метрика, по которой модели
отбирались при обучении. Таким образом, метрика отбора и целевая диагностика
(корреляция S(q,ω) на длинном прогоне) не выровнены: модель, «лучшая» по короткой
метрике отбора, не является лучшей по фактической длинной динамике. Это ограничение
процедуры отбора, а не вывода об избыточности памяти — по всем длинным диагностикам
однокадровая модель либо лучше, либо неотличима.

**Интерпретация.** Для марковского кристалла под EAM локальное мгновенное
окружение полностью определяет силу; история не несёт дополнительной информации и
лишь добавляет степени свободы, которые ухудшают сохранение энергии и длинную
динамику. Ответ на побочный вопрос, таким образом, отрицательный: память
избыточна, что лишь **подтверждает достаточность простой базовой модели**. Это не
отменяет память как механизм:
она должна проявляться в немарковских режимах (сильное трение, крупнозернистые
модели, явные немарковские ванны), описываемых обобщённым уравнением Ланжевена и
формализмом Мори–Цванцига, — но не в бездиссипативной динамике идеального
кристалла, рассмотренной здесь.

### Механический якорь: равновесное давление и упругий тензор

Заголовочная модель воспроизводит *динамику*. Мы отдельно проверяем,
восстанавливает ли та же force-only конструкция *механические* отклики,
дообучив её с вириальным якорем (п. 5a). Проверка двухступенчатая:
(i) равновесное давление в узлах решётки и (ii) полный кубический тензор
упругости.

**Равновесное давление.** Без якоря force-only модель даёт большое ложное
давление в равновесном репере ($\approx-70$ ГПа), и решётка не является
минимумом по объёму. Один вириальный член $\mathcal{L}_{\text{anchor}}$
($w=10^4$, цель $P=0$) убирает ложное давление до $P(a_0)=-0.7$ ГПа, не
портя динамику (корреляция $S(q,\omega)=0.930$ против $0.938$ без якоря).
Это чистый in-distribution результат: он вычисляется ровно на узлах
решётки ($\mathbf{u}=0$).

**Упругий тензор из акустических фононов.** Строгий модуль нельзя получить
статическим сканом $E(a)$: при фиксированном репере масштабирование
$a\to a\,s$ подаёт в сеть аффинное когерентное поле смещений
$\delta=\mathbf{R}^{\text{ref}}(s-1)$, которое на краях разумного окна
($s=1\pm0.04$) в $\sim\!9$ раз превышает обучающую тепловую амплитуду
($\delta_{\text{RMS}}\approx0.11$ Å при 300 K) и относится к
непредставленной в обучении моде. Такой скан даёт лишь экстраполяционную и
шумную оценку. Вместо этого мы измеряем упругий тензор **в самой рабочей
300 K-траектории**, из наклонов акустических ветвей при малых $q$: скорость
звука $v=\mathrm{d}\omega/\mathrm{d}q|_{q\to0}$ даёт $\rho v^2$ — нужную
комбинацию $C_{ij}$. Это тепловые фононы той же амплитуды, что и обучение
(in-distribution). Продольный ток вдоль $[100]$ даёт $C_{11}$, поперечный
вдоль $[100]$ — $C_{44}$, поперечный вдоль $[110]$ с поляризацией
$[1\bar{1}0]$ — $(C_{11}-C_{12})/2$, откуда $C_{12}$ (табл. 2, рис. 4).

| Источник | $C_{11}$ | $C_{12}$ | $C_{44}$ | $B$ |
|----------|---------:|---------:|---------:|----:|
| **Модель (МОП + якорь)** | $154$ | $109$ | $72$ | $\mathbf{124}$ |
| Эталон MD (то же окно) | $170$ | $126$ | $72$ | $141$ |
| Эксперимент, Cu (300 K) | $168$ | $121$ | $75$ | $137$ |

*Таблица 2. Упругие константы (ГПа), извлечённые из наклонов акустических
фононов в 300 K NVT-траектории (Cu FCC, 1000 атомов). Фиты дисперсий
линейны ($r^2=0.99$–$1.0$); $C_{44}$ согласуется по двум независимым
ветвям (T$[100]$ и T$[110]$). Модель — заголовочный вход `ref-plus-delta`,
дообученный с якорем давления ($w=10^4$).*

Два вывода. Во-первых, **тот же force-only генератор силы, снабжённый
одним равновесным условием, даёт количественно верную упругость**:
$B=124$ ГПа против $141$ у гоняющего MD-эталона и $137$ в эксперименте —
согласие в пределах $\sim\!10\%$, причём все три независимые константы
воспроизведены. Во-вторых, **соотношение Коши нарушено правильно**:
модель даёт $C_{12}=109\neq C_{44}=72$, ровно как эталон ($126$ vs $72$) и
реальная медь ($121$ vs $75$). Это возможно потому, что парная энергия
здесь — скаляр от *полного вектора* связи (опорного $\mathbf{R}^{\text{ref}}$
и смещения $\boldsymbol{\delta}$ как трёхмерных величин), а не только от её
длины: поперечная составляющая смещения меняет энергию связи. Тем самым
представление воспроизводит нецентральность упругого отклика меди, оставаясь
формально парным, — соотношение Коши $C_{12}=C_{44}$, обязательное для
строго центрального парного потенциала, здесь корректно снято.

![Рис. 4. Упругий тензор Cu FCC из акустических фононов (1000 атомов, 300 K NVT). (a) Наклоны акустических ветвей ω(q) при малых q: модель (сплошные) против эталонного MD (штриховые) для продольной L[100], поперечной T[100] и поперечной T[110] ветвей. (b) Извлечённые упругие константы — модель, эталон MD и эксперимент; $C_{12}\neq C_{44}$ показывает воспроизведённое нарушение соотношения Коши.]({{artifact:art_9ba6bae4-8216-4741-85f3-0f7c3842fdef}})

## Обсуждение и ограничения

Полученные результаты следует интерпретировать в границах поставленного
эксперимента.

**Одна система и один эталон.** Валидация выполнена на единственной системе — FCC
меди — и относительно эталона EAM, а не расчётов теории функционала плотности (DFT).
Это осознанный выбор: EAM даёт точную, воспроизводимую и дешёвую эталонную
динамику, что позволяет чисто изолировать методический вопрос (структура силы,
роль эквивариантности и памяти) от шума аппроксимации поверхности энергии. Прямым
следствием является то, что мы не заявляем DFT-точности и не проверяем перенос на
дефекты, фазовые границы или другие решётки — это направление дальнейшей работы.

**Немарковость не протестирована.** Наш вывод об избыточности памяти строго
относится к бездиссипативной динамике идеального марковского кристалла. Мы не
проверяли режимы, где память физически необходима (сильное трение, крупнозернистые
координаты, явные немарковские ванны); там ожидаемо проявился бы противоположный
результат, и рекуррентное ядро, введённое здесь, — естественный инструмент для
такой проверки.

**Рассогласование метрики отбора и цели.** Модели отбирались по короткой метрике
`mean_norm` (L2-расстояние S(q,ω) на коротком rollout), которая, как оказалось, не
согласована с целевой длинной диагностикой (корреляция S(q,ω) на 100 пс): модель,
«лучшая» по метрике отбора, не является лучшей по фактической длинной динамике. Это
ограничение процедуры отбора; согласование метрики отбора с длинными наблюдаемыми —
отдельная задача.

**Абсолютная скорость не измерена.** Оценки числа параметров и стоимости шага —
аналитические (плотные матричные умножения ≤ 256×256, без сферических гармоник);
строгое утверждение о скорости потребовало бы прямого бенчмарка против
эквивариантных потенциалов на одинаковом оборудовании, чего мы здесь не делаем.

**Механика — отдельная постановка, не цель работы.** Восстановление
равновесного давления и упругого тензора (п. 5a, «Механический якорь»)
показано как контролируемое расширение force-only конструкции, а не как её
основная цель. Важно, что упругие константы измерены *внутри обучающего
распределения* — из тепловых акустических фононов рабочей 300 K-траектории, а
не статическим сканом $E(a)$, который уводил бы деформацию на $\sim\!9$ раз за
пределы обучающих амплитуд в непредставленную моду и давал лишь
экстраполяционную оценку. Ограничение постановки в другом: и упругость, и
динамика проверены при одной температуре и относительно одного (EAM) эталона;
перенос упругого отклика на другие температуры, объёмы и на DFT-эталон — предмет
дальнейшей работы.

## Заключение

Мы показали, что для молекулярной динамики кристалла достаточно минимального
консервативного парного генератора силы, и сформулировали шесть вкладов:

1. **Минимальный консервативный парный генератор.** Чётный скалярный парный
   потенциал, из которого силы берутся градиентом; энергия существует только через
   градиент, обучение — только по силам, без энергетических меток.
2. **Осознанный отказ от эквивариантности через решёточный репер.** При
   фиксированной ориентации кристалла вращательная симметрия спонтанно нарушена,
   решётка задаёт репер, а глобальные повороты не сэмплируются; поэтому
   эквивариантность не приближается (в отличие от усреднения по реперам и
   канонизации), а признаётся избыточной — что даёт простой вход и компактную
   дешёвую модель.
3. **Единица разложения — трансляционная ячейка, а не атом.** В отличие от
   стандартного атомоцентрированного разложения $E=\sum_i E_i$, скользящее окно
   3×3×3 предсказывает силы сразу для всей центральной ячейки и движется по
   трансляциям решётки; тот же парный оператор переносится с суперячейки 3×3×3 на
   систему из 1000 атомов без дообучения.
4. **Валидация по коллективной динамике.** Модель проверяется по S(q,ω) и
   сопутствующим наблюдаемым, а не по ошибке силы.
5. **Память как контролируемый результат.** Абляция показывает, что для
   марковского кристалла история избыточна, и связывает это с режимами
   GLE / Мори–Цванцига, где память должна работать.
6. **Механический якорь: от сил к упругому тензору.** Один вириальный член в
   потере ($w=10^4$, цель $P=0$ в равновесном репере), без энергетических меток,
   устраняет ложное равновесное давление ($-70\to-0.7$ ГПа) и делает решётку
   устойчивым минимумом по объёму, почти не влияя на динамику
   ($S(q,\omega)$ $0.938\to0.930$). Снятый затем *внутри обучающего
   распределения* — из наклонов тепловых акустических фононов — полный кубический
   тензор упругости согласуется с эталоном в пределах $\sim\!10\%$ ($B=124$ против
   $141$ ГПа) и правильно воспроизводит нарушение соотношения Коши
   $C_{12}\neq C_{44}$, то есть нецентральность упругого отклика меди.

Естественные продолжения — перенос на DFT-эталон и на дефектные/многофазные
системы, а также проверка рекуррентного ядра в явно немарковских постановках, где
ожидается, что память перестанет быть избыточной.

## Доступность кода и данных

Код модели, обучающие скрипты и конфигурации инференса, а также скрипты построения
диагностик (S(q,ω), RDF, дрейф энергии, скорость звука, теплоёмкость) доступны в
репозитории проекта [ссылка будет добавлена при подаче]. Эталонные
EAM-траектории генерируются штатными средствами LAMMPS/ASE с потенциалом
`Cu_u3.eam`; параметры прогонов (суперячейка, термостат Bussi τ = 200 фс, T = 300 K,
шаг 2 фс, длительность) приведены в разделе «Методы» и достаточны для полного
воспроизведения.

## Литература

1. Behler J., Parrinello M. *Generalized Neural-Network Representation of High-Dimensional Potential-Energy Surfaces*. Physical Review Letters, 98, 146401 (2007). https://doi.org/10.1103/PhysRevLett.98.146401
2. Zhang L., Han J., Wang H. et al. *Deep Potential Molecular Dynamics: A Scalable Model with the Accuracy of Quantum Mechanics*. Physical Review Letters, 120, 143001 (2018). https://doi.org/10.1103/PhysRevLett.120.143001
3. Schütt K.T., Kindermans P., Sauceda H.E. et al. *SchNet: A continuous-filter convolutional neural network for modeling quantum interactions*. arXiv:1706.08566 (2017). https://arxiv.org/abs/1706.08566
4. Batzner S., Musaelian A., Sun L. et al. *E(3)-equivariant graph neural networks for data-efficient and accurate interatomic potentials*. Nature Communications, 13, 2453 (2022). https://doi.org/10.1038/s41467-022-29939-5
5. Musaelian A., Batzner S., Johansson A. et al. *Learning local equivariant representations for large-scale atomistic dynamics*. Nature Communications, 14, 579 (2023). https://doi.org/10.1038/s41467-023-36329-y
6. Batatia I., Kovács D.P., Simm G.N.C. et al. *MACE: Higher Order Equivariant Message Passing Neural Networks for Fast and Accurate Force Fields*. arXiv:2206.07697 (2022). https://arxiv.org/abs/2206.07697
7. Chen C., Ong S.P. *A universal graph deep learning interatomic potential for the periodic table*. Nature Computational Science, 2, 718-728 (2022). https://doi.org/10.1038/s43588-022-00349-3
8. Deng B., Zhong P., Jun K. et al. *CHGNet as a pretrained universal neural network potential for charge-informed atomistic modelling*. Nature Machine Intelligence, 5, 1031-1041 (2023). https://doi.org/10.1038/s42256-023-00716-3
9. Yang H., Hu C., Zhou Y. et al. *MatterSim: A Deep Learning Atomistic Model Across Elements, Temperatures and Pressures*. arXiv:2405.04967 (2024). https://arxiv.org/abs/2405.04967
10. Neumann M., Gin J., Rhodes B. et al. *Orb: A Fast, Scalable Neural Network Potential*. arXiv:2410.22570 (2024). https://arxiv.org/abs/2410.22570
11. Tsai S., Kuo E., Tiwary P. *Learning molecular dynamics with simple language model built upon long short-term memory neural network*. Nature Communications, 11, 5115 (2020). https://doi.org/10.1038/s41467-020-18959-8
12. Xie P., Car R., E W. *Ab initio generalized Langevin equation*. Proceedings of the National Academy of Sciences, 121, e2308668121 (2024). https://doi.org/10.1073/pnas.2308668121
13. Hjorth Larsen A., Mortensen J. J., Blomqvist J. et al. *The atomic simulation environment—a Python library for working with atoms*. Journal of Physics: Condensed Matter, 29, 273002 (2017). https://doi.org/10.1088/1361-648X/aa680e
14. Bussi G., Donadio D., Parrinello M. *Canonical sampling through velocity rescaling*. The Journal of Chemical Physics, 126, 014101 (2007). https://doi.org/10.1063/1.2408420
15. Thompson A., Aktulga H., Berger R. et al. *LAMMPS - a flexible simulation tool for particle-based materials modeling at the atomic, meso, and continuum scales*. Computer Physics Communications, 271, 108171 (2022). https://doi.org/10.1016/j.cpc.2021.108171
16. Daw M., Baskes M. *Embedded-atom method: Derivation and application to impurities, surfaces, and other defects in metals*. Physical Review B, 29, 6443-6453 (1984). https://doi.org/10.1103/PhysRevB.29.6443
17. Paszke A., Gross S., Massa F. et al. *PyTorch: An Imperative Style, High-Performance Deep Learning Library*. arXiv:1912.01703 (2019). https://arxiv.org/abs/1912.01703
18. Chmiela S., Tkatchenko A., Sauceda H.E. et al. *Machine learning of accurate energy-conserving molecular force fields*. Science Advances, 3, e1603015 (2017). https://doi.org/10.1126/sciadv.1603015
19. Wang J., Olsson S., Wehmeyer C. et al. *Machine Learning of Coarse-Grained Molecular Dynamics Force Fields*. ACS Central Science, 5, 755-767 (2019). https://doi.org/10.1021/acscentsci.8b00913
20. Puny O., Atzmon M., Ben-Hamu H. et al. *Frame Averaging for Invariant and Equivariant Network Design*. arXiv:2110.03336 (2021). https://arxiv.org/abs/2110.03336
21. Duval A., Schmidt V., Hernández-García A. et al. *FAENet: Frame Averaging Equivariant GNN for Materials Modeling*. arXiv:2305.05577 (2023). https://arxiv.org/abs/2305.05577
22. Kaba S.-O., Mondal A.K., Zhang Y. et al. *Equivariance with Learned Canonicalization Functions*. arXiv:2211.06489 (2022). https://arxiv.org/abs/2211.06489
23. Elhag A.A., Raja A., Morehead A. et al. *Learning Inter-Atomic Potentials without Explicit Equivariance*. arXiv:2510.00027 (2025). https://arxiv.org/abs/2510.00027
24. Park C.W., Kornbluth M., Vandermause J. et al. *Accurate and scalable graph neural network force field and molecular dynamics with direct force architecture*. npj Computational Materials, 7, 73 (2021). https://doi.org/10.1038/s41524-021-00543-3