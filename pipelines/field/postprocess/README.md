# Field Postprocessing

FieldRNN uses the shared postprocessing tools in `../../shared/postprocess`.
Field-specific rollout extraction is available through the legacy root script
`save_field_rnn_selection_rollout.py`; it can be moved here once the root
compatibility layer is retired.
